import java.util.List;
import java.util.Scanner;
import java.util.logging.Logger;

public class Main5_Array {

    private static Logger logger = Logger.getLogger("Mylogger");

    public static void main(String[] args) {


        // 백준 단계별로 풀어보기

        // 5. Array
        // 5-1
        // logger.info("5-1 : 시스탬 입력, Array, 최소값 최대값");
        // BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        // String input1 = br.readLine();
        // String input2 = br.readLine();
        // int count = Integer.parseInt(input1);
        // String[] inputArray = input2.split(" ");
        // int min = Integer.parseInt(inputArray[0]);
        // int max = Integer.parseInt(inputArray[0]);
        // for (int i = 0; i < count; i++) {
        //     //최소값
        //     if (min > Integer.parseInt(inputArray[i])) {
        //         min = Integer.parseInt(inputArray[i]);
        //     }
        //     //최대값
        //     if (max < Integer.parseInt(inputArray[i])) {
        //         max = Integer.parseInt(inputArray[i]);
        //     }
        // }
        // System.out.println(min + " " + max);
        
    }
}




 